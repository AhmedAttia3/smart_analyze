name: smart_analyze
description: A clean Flutter analyzer that filters for errors and supports specific file analysis.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.4.2

executables:

  smart_analyze: smart_analyze
